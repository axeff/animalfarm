<?
  $name = 'artists';
  spl_autoload_register(function ($name) {
    echo "Want to load $name.\n";
    throw new Exception("Unable to load $name.");
  });

  include_once( dirname( __DIR__ ) . '/../repositories/artists.php' );
  $artists = new Artists();
?>
<div class="o-pack o-pack--auto o-pack--middle">
  <div class="o-pack__item palm-one-whole pl+">
    <h3 class="m0 clear palm-p0 palm-m0 left"><span>LINEUP</span></h3>
  </div><!--
  --><div class="o-pack__item palm-one-whole">
      <div class="o-pack">
        <?
          $i = 0;
          foreach ($artists->getAll() as $artist) {
            $i++;
            $name = $artist['name'];
            if (count($artist['link'])){
              $name = '<a target="_blank" class="ph no-deco" href="'.$artist['link'].'">'.$name.'</a>';
            }
            echo "<div class='o-pack__item pv- left font__amatic delta left'>".$name."</div>";

            if ($i % 5 == 0) {
              echo '</div><div class="o-pack">';
            }

          }
        ?>
      </div>
  </div>
  <div class="o-pack__item palm-one-whole">
    <div class="vertical bold mr gamma">MORE TBA.</div>
  </div>
</div>
